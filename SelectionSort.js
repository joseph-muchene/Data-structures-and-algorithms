// [8,7,6,5,4,3,2,1]
function SelectionSort(arr) {
    for(let x = 1; x < arr.length; x++){
        let val = arr[i]
        let j = i - 1;

        while(j >= 0 && val < arr[j]){
         arr[j + 1] = arr[j]
        }
    }
return arr 
}

