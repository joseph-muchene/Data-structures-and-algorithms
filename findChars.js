// write a function which takes in string and returns the count of each character in the string

function findCountChars() {
 let str = "hello"
//  transform str into an array
const transformed = str.split("")
// loop each character and find if it equanls the previous one
for(let x = transformed.length - 1; x >= 0;x--){
  if(transformed)
}
// if it equals the previous store it in a dictionary containing the count and the value

 
}
findCountChars();
